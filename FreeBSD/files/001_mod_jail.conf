# To enable, uncomment the LoadModule line

# LoadModule http2_module %%APACHE_MODDIR%%/mod_jail.so

<IfModule jail_module>
	jail_rootdir  "/"
	jail_hostname  "localhost"
	jail_address  127.0.0.1
	jail_address6  ::1
	jail_scrlevel  3
</IfModule>
